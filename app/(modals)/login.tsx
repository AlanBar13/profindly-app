import { View, Text } from "react-native";
import React from "react";
import { Colors } from "@/constants/Colors";

const Page = () => {
  return (
    <View>
      <Text style={{color: Colors.dark.primary}}>Login</Text>
    </View>
  );
};

export default Page;
